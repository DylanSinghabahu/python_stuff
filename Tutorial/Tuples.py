numbers = (1, 2, 3) #parathensis for tuples and squarebrackets for lists
#Tuples are immutable, meaning it can't be modified, hence why there is no remove,clear or pop commands

print(numbers[0]) #but you can't do numbers[0] = 10 as it is immutable